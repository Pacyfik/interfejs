<p id="notice"><%= notice %></p>

<h1>Twoja lista etykiet</h1>

<table>
  <tr>
    <th>Nazwa <%= link_to '&darr;'.html_safe, etykiety_path(:order => "asc") %> <%= link_to '&uarr;'.html_safe, etykiety_path(:order => "desc") %></th>
    <th>Obrazek</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tags.each do |tag| %>
  <tr>
    <td><%= tag.name %></td>
    <td><%= tag.picture %></td>
    <td><%= link_to 'Szczegóły', tag %></td>
    <td><%= link_to 'Edycja', edit_tag_path(tag) %></td>
    <td><%= link_to 'Usuń', tag, method: :delete, data: { confirm: 'Jesteś pewien?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Dodaj nową etykietę', etyk_nowa_path %>
