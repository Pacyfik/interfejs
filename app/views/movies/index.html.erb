<h1>Listing movies</h1>

<table>
  <tr>
    <th>Country</th>
    <th>Genre</th>
    <th>Id2</th>
    <th>Overview</th>
    <th>Poster path</th>
    <th>Title</th>
    <th>Year</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @movies.each do |movie| %>
  <tr>
    <td><%= movie.country %></td>
    <td><%= movie.genre %></td>
    <td><%= movie.id2 %></td>
    <td><%= movie.overview %></td>
    <td><%= movie.poster_path %></td>
    <td><%= movie.title %></td>
    <td><%= movie.year %></td>
    <td><%= link_to 'Show', movie %></td>
    <td><%= link_to 'Edit', edit_movie_path(movie) %></td>
    <td><%= link_to 'Destroy', movie, method: :delete, data: { confirm: 'Jesteś pewien?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Movie', new_movie_path %>
